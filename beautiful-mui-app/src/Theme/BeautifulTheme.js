import { createTheme } from "@mui/material/styles";

export const BeautifulTheme = createTheme({
  palette: {
    primary: {
      main: "#18842c",
      light: "#3aab58",
      dark: "sienna",
    },
    grid: {
      main: "rgba(0,0,0,0.1)",
      dark: "rgba(0,0,0,0.2)",
    },
  },
});
